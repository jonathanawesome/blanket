export const something = (() => {
  const body = document.body;

  const init = () => {
    //initialize
  };

  const _getSomething = () => {
    //private
  };

  return {
    init: init,
  };
})();
